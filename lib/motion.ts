/** Shared motion constants. The cubic-bezier is typed as a 4-tuple so it
 *  satisfies Framer Motion's `Easing` type (a plain number[] does not). */
export const EASE_OUT_EXPO = [0.16, 1, 0.3, 1] as [number, number, number, number];
